<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.lina.pengenalanpakaianadatnusantaran">

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher2"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher2_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SkorActivity"></activity>
        <activity android:name=".GameActivity" />
        <activity android:name=".KategoriGameActivity" />
        <activity android:name=".MateriPakaianAdatPengantinActivity" />
        <activity android:name=".MateriPakaianAdatActivity" />
        <activity android:name=".KategoriMateriActivity" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".HomeActivity" />
        <activity
            android:name=".SplashActivity"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>